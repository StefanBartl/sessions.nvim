name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  stylua:
    name: stylua (format check)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pinned, not "latest": stylua's own rule changes between releases would
      # otherwise turn this gate red with no local diff to blame it on.
      - uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: 2.5.2
          args: --check lua TESTS

  luacheck:
    name: luacheck (lint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Plain Lua 5.4, not LuaJIT: luacheck only needs *an* interpreter to run
      # on, and which dialect it lints for is set by `std` in .luacheckrc --
      # independent of the host. Pinning a LuaJIT build here buys nothing and
      # breaks when that exact tarball disappears upstream.
      - uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: "5.4"
      - uses: leafo/gh-actions-luarocks@v4
      - run: luarocks install luacheck
      - run: luacheck lua TESTS

  tests:
    name: tests (headless)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: sessions.nvim

      # lib.nvim is a real runtime dependency -- sessions.portable and others
      # require it at module load -- so the suite cannot run without it.
      # Checked out as a sibling, which is the layout TESTS/run.lua's own
      # resolver looks for first after $LIB_NVIM_PATH.
      - uses: actions/checkout@v4
        with:
          repository: StefanBartl/lib.nvim
          path: lib.nvim

      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable

      - name: Run TESTS/run.lua
        working-directory: sessions.nvim
        run: nvim --headless -u NONE -c "set rtp+=." -l TESTS/run.lua
